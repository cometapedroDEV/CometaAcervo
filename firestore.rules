rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // AVISO: Regras abertas para desenvolvimento conforme solicitado.
    // Em produção, estas regras devem ser restritas.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}